//! Workspace root for the Sextant suite. This crate exists so the workspace
//! Cargo.toml has a `[package]` block for release-please to bump; member
//! crates under `crates/` carry the real functionality.
