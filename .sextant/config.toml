# Sextant config for the Sextant repo. This is the canonical example
# of what a project ships in `.sextant/config.toml`.

[verdict]
max_errors = 0
max_warns = 50

[size]
file_length_warn = 400
file_length_error = 800
