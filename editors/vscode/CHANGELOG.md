# Changelog

## 0.1.0

- Initial release.
- Live diagnostics on `did_change` (debounced) and `did_save`.
- Hover-to-explain showing the full rule body.
- `sextant.disableLlm` (default true) and `sextant.serverPath` settings.
